# Documentation

```
~/monitoring.sh
/etc/login.defs
/etc/ssh/sshd_config
```

## Commandes

Install UFW
```
sudo ufw allow 4242
sudo ufw enable
sudo ufw status verbose
```

Get passwd info
```
sudo chage -l $USER 
```
